# graphql-react-todos

## Requirements
* Node
* Npm
* Docker
* Docker Compose

## Startup
Up services with:
```bash
docker-compose up -d
```

Install and run application with:
```bash
npm i && npm start
```